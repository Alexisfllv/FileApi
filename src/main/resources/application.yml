server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/fileapi
    username: admin
    password: secreta123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true